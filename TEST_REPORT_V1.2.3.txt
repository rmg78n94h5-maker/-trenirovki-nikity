ПРОВЕРКА РЕЛИЗА v1.2.3 — «УДОБНЫЙ ВВОД»

Статические проверки:
- синтаксис version.js, seed.js, db.js, exercise-guides.js, app.js и service-worker.js: успешно;
- manifest.webmanifest: корректный JSON;
- повторяющихся объявлений функций в app.js не найдено;
- все 13 ресурсов APP_SHELL существуют;
- единая версия приложения: 1.2.3;
- кэш Service Worker: nikita-workouts-v1.2.3-convenient-input;
- маркер Service Worker: 1.2.3, version.js загружается с параметром релиза и проверяется на совпадение;
- IndexedDB остаётся версии 2;
- функция подстановки проверена отдельно: если завершённых подходов было меньше, последнее завершённое значение повторяется для оставшихся подходов.

Сценарии в Chromium, экран 390×844:
- первый запуск и создание профиля;
- запуск тренировки с 11 упражнениями;
- кнопки −/＋ для минут и секунд;
- кнопки −/＋ для веса с шагом 0,5 кг;
- кнопки −/＋ для повторов с шагом 1;
- ручной ввод числа в центральное поле;
- удержание кнопки для быстрого изменения значения;
- сохранение выполненных подходов с разными весами и повторами;
- повторный запуск того же тренировочного дня;
- точная подстановка веса и повторов каждого завершённого подхода из последнего выполнения;
- добавленный подход копирует значения предыдущего текущего подхода;
- черновик сохраняет значения нового интерфейса;
- версия в интерфейсе: 1.2.3;
- фактическая база в Chromium: nikita-workouts-db v2;
- необработанных ошибок страницы и console.error не обнаружено.

Проверка обновления v1.2.2 → v1.2.3, экран 320×720:
- профиль, созданный в v1.2.2, сохранён;
- черновик v1.2.2 восстановлен в v1.2.3;
- вручную введённый вес 21,5 кг сохранён;
- IndexedDB осталась версии 2;
- горизонтальной прокрутки на ширине 320 px нет.

Конфигурация Service Worker проверена статически: основной файл изменён, имя кэша повышено, все файлы оболочки присутствуют. Фактическую установку Service Worker на iPhone нужно подтвердить первым открытием опубликованной версии при наличии интернета.

Проверка выполнена 17.07.2026.
