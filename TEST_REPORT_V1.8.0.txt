# TEST REPORT v1.8.0 — Калькулятор железа

## Проверки

- `node --check app.js` — OK.
- `node --check db.js` — OK.
- `node --check seed.js` — OK.
- `node --check exercise-guides.js` — OK.
- `python3 -m json.tool manifest.webmanifest` — OK.
- Проверена версия в `version.js`: 1.8.0.
- Проверен маркер Service Worker: 1.8.0.
- Проверены query-параметры в `index.html`: 1.8.0.
- Проверен новый cache name: `nikita-workouts-v1.8.0-iron-calculator`.

## Что проверено по логике

- Карточка «Калькулятор железа» добавлена в раздел «Ещё».
- Модальное окно калькулятора открывается из раздела «Ещё».
- Быстрая кнопка калькулятора добавлена на экран тренировки.
- Расчёт поддерживает режимы «Гантель» и «Штанга».
- Блины считаются симметрично на две стороны.
- При невозможности точного веса показывается ближайший вариант.
- Настройки сохраняются через существующий механизм `DB.setSettingsObject`.

## IndexedDB

- Версия базы остаётся v3.
- Новые objectStore не добавлялись.
- Миграция не требуется.
- Пользовательские данные не очищаются.
