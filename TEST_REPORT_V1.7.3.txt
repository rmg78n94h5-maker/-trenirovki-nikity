# Проверка v1.7.3 — графики прогресса тела

Проверено статически:
- app.js: синтаксис корректен через node --check.
- db.js: синтаксис корректен через node --check.
- seed.js: синтаксис корректен через node --check.
- exercise-guides.js: синтаксис корректен через node --check.
- manifest.webmanifest: JSON валиден.
- version.js и service-worker.js используют версию 1.7.3.
- index.html подключает JS/CSS с параметром v=1.7.3.
- Все файлы из APP_SHELL service-worker.js существуют.
- ZIP проверен после сборки.

Что важно проверить на iPhone после загрузки на GitHub:
1. Прогресс → Тело.
2. Переключение показателей: Вес / Талия / Живот / Грудь / Бёдра / Рука.
3. Переключение периодов: 7 / 30 / 90 дней / Всё.
4. График должен быть крупнее, с плавающей шкалой и блоком «Было / Стало / Разница».
5. При старом кэше использовать Ещё → Обновление приложения → Обновить кэш.

Интерактивный iPhone/PWA-тест в sandbox не выполнялся.
