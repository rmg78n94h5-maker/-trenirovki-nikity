# Проверка v1.7.6 — Sport Premium Plan

## Автопроверки

- app.js: синтаксис проверен через node --check.
- db.js: синтаксис проверен через node --check.
- seed.js: синтаксис проверен через node --check.
- exercise-guides.js: синтаксис проверен через node --check.
- manifest.webmanifest: JSON валиден.
- version.js: версия 1.7.6.
- service-worker.js: SERVICE_WORKER_RELEASE = 1.7.6.
- index.html: query-параметры скриптов и CSS обновлены до v=1.7.6.
- APP_SHELL: все файлы из списка Service Worker существуют.
- upload-only ZIP: содержит только файлы, необходимые для загрузки.

## Ручная логика изменений

- Раздел «План» рендерится через новую премиальную hero-карточку.
- Переключатели программ остались рабочими.
- Кнопки создания, дублирования и добавления дней оставлены с прежними ID.
- Кнопки редактирования, старта дня и назначения текущим используют прежние классы обработчиков.
- Полные списки упражнений скрыты в раскрываемый блок, чтобы экран не превращался в длинную простыню.

## Данные

Миграция IndexedDB не выполнялась. Данные пользователя не изменяются.
