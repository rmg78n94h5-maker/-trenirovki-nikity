# TEST REPORT v1.8.1 — Фикс фильтров прогресса

## Проверки синтаксиса и файлов

- `node --check app.js` — OK.
- `node --check db.js` — OK.
- `node --check seed.js` — OK.
- `node --check exercise-guides.js` — OK.
- `node --check version.js` — OK.
- `node --check service-worker.js` — OK.
- `python3 -m json.tool manifest.webmanifest` — OK.
- Query-параметры ресурсов в `index.html` обновлены до 1.8.1.
- Версия в `version.js` — 1.8.1.
- Маркер Service Worker — 1.8.1.
- Новый cache name — `nikita-workouts-v1.8.1-progress-filter-fix`.

## Проверка исправления

- Для активного показателя прогресса добавлен финальный CSS-приоритет с тёмным текстом `#080a0c` на лаймовом фоне.
- Для активного периода добавлен тот же контрастный стиль.
- Правило размещено после конфликтующего Sport Premium-правила и имеет равную или более высокую специфичность, поэтому перекрывает ошибочный лаймовый цвет текста.
- Неактивные фильтры не изменены.
- Активные вкладки программ не изменены.

## IndexedDB и данные

- `db.js`, `app.js`, `seed.js` и `exercise-guides.js` не менялись.
- Версия IndexedDB остаётся v3.
- Миграция не требуется.
- Профили, программы, тренировки, замеры, фото, боль, рекорды и настройки не очищаются.
